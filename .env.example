# AWS credentials for S3 access — copy to .env and fill in real values
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ap-south-1

# S3 location of the telemetry data
S3_BUCKET=
S3_PREFIX=

# Local folder to download into
DATA_DIR=data
